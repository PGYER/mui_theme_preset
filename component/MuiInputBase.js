const MuiInputBase = (theme, palette) => {
  return {
    input: {
      fontSize: 14,
      color: palette.text.main,
      height: '19px',
      lineHeight: '19px',
      backgroundColor: palette.background.light,
      borderRadius: '4px'
    }
  }
}

export default MuiInputBase
