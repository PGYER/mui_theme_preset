const MuiDialogTitle = (theme, palette) => {
  return {
    root: {
      padding: theme.spacing(2.5) + 'px ' + theme.spacing(3) + 'px',
      textAlign: 'center'
    }
  }
}

export default MuiDialogTitle
