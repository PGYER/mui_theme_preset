const MuiListSubheader = (theme, palette) => {
  return {
    root: {
      opacity: '0.8',
      lineHeight: theme.spacing(4) + 'px',
      fontSize: '12px'
    }
  }
}

export default MuiListSubheader
