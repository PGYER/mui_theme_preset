const MuiListItemIcon = (theme, palette) => {
  return {
    root: {
      color: palette.text.light,
      marginRight: 0,
      minWidth: theme.spacing(4)
    }
  }
}

export default MuiListItemIcon
